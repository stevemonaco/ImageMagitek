namespace TileShop.Shared.Interactions;

/// <summary>
/// How much of the window a dialog claims
/// </summary>
public enum DialogSize
{
    /// <summary>Sized to content within a modest maximum width</summary>
    Default,

    /// <summary>Fills the window save for a margin, for content that needs the space such as image previews</summary>
    Large
}
