<UserControl x:Class="TileShop.WPF.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TileShop.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Menu FontSize="18">
        <MenuItem Header="_File">
            <MenuItem Header="_New" />
            <MenuItem x:Name="OpenProject" Header="Open _Project"/>
            <MenuItem x:Name="CloseProject" Header="Close Project" />
            <Separator />
            <MenuItem Header="_Save Project" />
            <MenuItem Header="_Save Project As..." />
            <Separator />
            <MenuItem x:Name="ExitApplication" Header="E_xit" />
        </MenuItem>

        <MenuItem Header="_Add">
            <MenuItem x:Name="AddPalette" Header="Palette Definition..." />
            <MenuItem x:Name="AddScatteredArranger" Header="Scattered Arranger Definition..." />
        </MenuItem>

        <MenuItem Header="_Plugins">
            
        </MenuItem>
    </Menu>
</UserControl>
