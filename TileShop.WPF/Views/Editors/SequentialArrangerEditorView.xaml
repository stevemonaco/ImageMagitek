<UserControl
    x:Class="TileShop.WPF.Views.SequentialArrangerEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:TileShop.WPF.Behaviors"
    xmlns:cm="http://caliburnmicro.com"
    xmlns:converters="clr-namespace:TileShop.WPF.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:local="clr-namespace:TileShop.WPF.Views"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:TileShop.WPF.Models"
    cm:Message.Attach="[Event Closing] = [Action OnClose($eventArgs)]"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.InputBindings>
        <KeyBinding Key="OemPlus" Command="{Binding MoveByteDown}" />
        <KeyBinding Key="Add" Command="{Binding MoveByteDown}" />
        <KeyBinding Key="Subtract" Command="{Binding MoveByteUp}" />
        <KeyBinding Key="OemMinus" Command="{Binding MoveByteUp}" />
        <KeyBinding Key="Down" Command="{Binding MoveRowDown}" />
        <KeyBinding Key="Up" Command="{Binding MoveRowUp}" />
        <KeyBinding Key="Right" Command="{Binding MoveColumnRight}" />
        <KeyBinding Key="Left" Command="{Binding MoveColumnLeft}" />
        <KeyBinding Key="PageDown" Command="{Binding MovePageDown}" />
        <KeyBinding Key="PageUp" Command="{Binding MovePageUp}" />
        <KeyBinding Key="Home" Command="{Binding MoveHome}" />
        <KeyBinding Key="End" Command="{Binding MoveEnd}" />
        <KeyBinding Key="OemQuestion" Command="{Binding ExpandColumn}" />
        <KeyBinding Key="OemPeriod" Command="{Binding ShrinkColumn}" />
        <KeyBinding Key="L" Command="{Binding ExpandRow}" />
        <KeyBinding Key="OemSemicolon" Command="{Binding ShrinkRow}" />
        <KeyBinding Key="A" Command="{Binding SelectNextCodec}" />
        <KeyBinding Key="S" Command="{Binding SelectPreviousCodec}" />
        <KeyBinding Key="Esc" Command="{Binding CancelSelection}" />
        <KeyBinding Key="Z" Command="{Binding ZoomIn}" />
        <KeyBinding Key="X" Command="{Binding ZoomOut}" />
        <KeyBinding Key="G" Command="{Binding ToggleGridlineVisibility}" />
        <KeyBinding Key="J" Command="{Binding ShowJumpDialog}" />
    </UserControl.InputBindings>
    <Grid UseLayoutRounding="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Toolbar  -->
        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Margin="0,10,0,20"
            Orientation="Horizontal">
            <Slider
                MinWidth="100"
                Margin="10,0,10,0"
                Maximum="16"
                Minimum="1"
                Value="{Binding Zoom}" />
            <ToggleButton
                Margin="0,0,10,0"
                cm:Message.Attach="[Event Checked] = [Action ToggleGridlines];[Event Unchecked] = [Action ToggleGridlines]"
                Content="#"
                IsChecked="{Binding ShowGridlines}"
                Visibility="{Binding CanShowGridlines, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" />
            <ToggleButton
                Margin="0,0,10,0"
                Content="Snap"
                IsChecked="{Binding Selection.SnapMode, Converter={StaticResource SnapModeBooleanConverter}}"
                Visibility="{Binding CanChangeSnapMode, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" />
        </StackPanel>

        <!--  Image Display  -->
        <ScrollViewer
            Grid.Row="1"
            Grid.Column="0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.Resources>
                    <ScaleTransform x:Key="transform" ScaleX="{Binding Zoom}" ScaleY="{Binding Zoom}" />
                    <SolidColorBrush
                        x:Key="arrangeSelectionFillBrush"
                        Opacity="0.5"
                        Color="Maroon" />
                    <SolidColorBrush
                        x:Key="editSelectionFillBrush"
                        Opacity="0.5"
                        Color="LawnGreen" />
                </Grid.Resources>

                <i:Interaction.Behaviors>
                    <behaviors:MouseCaptureBehavior Proxy="{Binding}" />
                </i:Interaction.Behaviors>

                <Image
                    Grid.Row="0"
                    Grid.Column="0"
                    dd:DragDrop.DropHandler="{Binding}"
                    LayoutTransform="{StaticResource transform}"
                    RenderOptions.BitmapScalingMode="NearestNeighbor"
                    SnapsToDevicePixels="True"
                    Source="{Binding ArrangerSource}"
                    Stretch="None" />

                <!--  Selection Overlay  -->
                <Canvas
                    Grid.Row="0"
                    Grid.Column="0"
                    DataContext="{Binding Selection, Mode=TwoWay}"
                    LayoutTransform="{StaticResource transform}"
                    Visibility="{Binding HasSelection, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Hidden}">
                    <Rectangle
                        Canvas.Left="{Binding SnappedX1}"
                        Canvas.Top="{Binding SnappedY1}"
                        Width="{Binding SnappedWidth}"
                        Height="{Binding SnappedHeight}"
                        Fill="{StaticResource arrangeSelectionFillBrush}"
                        Stroke="Black"
                        StrokeDashArray="3 2"
                        StrokeThickness="0.3" />
                </Canvas>

                <!--  Gridline Overlay  -->
                <ItemsControl
                    Grid.Row="0"
                    Grid.Column="0"
                    ItemsSource="{Binding Gridlines}"
                    Visibility="{Binding ShowGridlines, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Hidden}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type model:Gridline}">
                            <Line
                                RenderOptions.EdgeMode="Aliased"
                                SnapsToDevicePixels="True"
                                Stroke="Black"
                                StrokeThickness="1"
                                UseLayoutRounding="True"
                                X1="{Binding X1}"
                                X2="{Binding X2}"
                                Y1="{Binding Y1}"
                                Y2="{Binding Y2}">
                                <Line.LayoutTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1" />
                                </Line.LayoutTransform>
                            </Line>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
